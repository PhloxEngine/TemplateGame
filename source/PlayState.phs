class PlayState : public State {
public:
    void Create() override {
        trace("PlayState created yeah haha");
        super.Create();
    }

    void Update(float elapsed) override {
        super.Update(elapsed);
    }

    void Render() override {
        super.Render();
    }

    void Destroy() override {
        super.Destroy();
    }
};